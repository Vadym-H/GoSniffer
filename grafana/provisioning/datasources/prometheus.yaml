# noinspection YAMLSchemaValidation
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-gosniffer
    access: proxy
    url: http://prometheus-dev:9090
    isDefault: true
    editable: false
